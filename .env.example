# Backend local (Express + SQLite)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="STARCORE SENTINEL <noreply@batenterprise.com>"
ADMIN_TOKEN=bat-sentinel-2026

# Netlify / Neon
# Configure estas variaveis no painel da Netlify.
NETLIFY_DATABASE_URL=
DATABASE_URL=
